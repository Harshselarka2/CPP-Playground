{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug C++ (gdb)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/${fileBasenameNoExtension}",
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false
    }
  ]
}
